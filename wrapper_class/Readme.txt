Integer i1 = Integer.valueof("Integer in format of string");
Integer i2 = i1.initValue();  Converts first object of Integer into primitive data type and then assigns to primitive int


Integer.valueOf() , Integer.parseInt() are static methods therefore don't need object.